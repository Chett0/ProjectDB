Execution: 

    flask --app app run


Migration: 

    $env:FLASK_APP="app" 
    flask db init
    flask db migrate
    flask db upgrade


Trigger:

    - il campo active viene messo a true, allora deletion_time deve essere nullo
    - Before deleting an aircraft?
    - se il deletion_time supera un certo limite allora va rimossa la riga?


SQL Injection cannot happen with SQLAlchemy

docker run -d --name redis -p 6379:6379 redis